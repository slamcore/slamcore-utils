name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:

        repo-token: ${{ secrets.GITHUB_TOKEN }}

        issue-message: 'Greetings to slamcore_utils and thanks for opening this issue'
        pr-message: 'Thanks for this contribution to slamcore_utils 🥳'

